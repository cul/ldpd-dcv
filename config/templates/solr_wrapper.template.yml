development:
  version: 6.3.0
  mirror_url: https://archive.apache.org/
  download_dir: <%= Rails.root.join('tmp', 'solr-download') %>
  verbose: false
  managed: true
  solr_options:
    "addlopts" : "-Xss1024k -Djetty.host=127.0.0.1"
  port: 8983
  instance_dir: <%= Rails.root.join('tmp', 'solr-development') %>
  collection:
    - name: 'dcv-dev'
      dir: <%= Rails.root.join('spec', 'fixtures', 'solr_cores', 'dcv-solr-6-3', 'conf') %>

test:
  version: 6.3.0
  mirror_url: https://archive.apache.org/
  download_dir: <%= Rails.root.join('tmp', 'solr-download') %>
  verbose: false
  managed: true
  solr_options:
    "addlopts" : "-Xss1024k -Djetty.host=127.0.0.1"
  port: 8993
  instance_dir: <%= Rails.root.join('tmp', 'solr-test') %>
  collection:
    - name: 'dcv-test'
      dir: <%= Rails.root.join('spec', 'fixtures', 'solr_cores', 'dcv-solr-6-3', 'conf') %>
