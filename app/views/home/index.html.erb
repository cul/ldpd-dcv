<div class="row">
  <div id="sidebar" class="col-md-3">
    <div class="inner">
      <p><strong>The Digital Library Collections (DLC)</strong> website is a gateway to digital projects and online exhibitions that have been published on the web by Columbia University Libraries / Information Services (CUL/IS). The site includes scans of photographs, posters, objects, manuscripts and other material from our special and archival collections. <a href="<%= about_url %>">Learn more &raquo;</a></p>
    </div>
  </div>

  <div id="content" class="col-md-9">
		<div id="home-page-content">

			<div id="home-page-tabs" class="inner">
				<!-- Nav tabs -->
				<ul class="nav nav-tabs">
					<li class="active"><a href="#sample_items" data-toggle="tab">Sample Items</a></li>
					<% @browse_lists.each do |facet_name, data| %>
						<li><a href="#tab_<%= facet_name %>" data-toggle="tab"><%= data['display_label'] %></a></li>
					<% end %>
					<li><a href="#projects" data-toggle="tab">Projects</a></li>
				</ul>

				<!-- Tab panes -->
				<div class="tab-content">
					<%= render template: 'home/sample_items' -%>
					<% @browse_lists.each do |facet_name, data| %>
						<%= render template: "pages/#{facet_name}", locals: {facet_name: facet_name, data: data} -%>
					<% end %>
					<div class="tab-pane fade" id="projects">
						<%= render template: 'pages/projects_and_exhibitions' -%>
					</div>
				</div>
			</div> <!--/.inner-->
		</div>
  </div>

</div>
