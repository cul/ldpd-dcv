<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <%= render partial: 'shared/head_html' %>
    <%= stylesheet_link_tag    controller.collection_layout %>
  </head>
  <body class="<%= render_body_class %> blacklight-<%= controller.collection_layout %> <%= params[:action] %>" onload="initTiles(<%= params[:initial_page] if params[:initial_page] %>);">

  <div id="main-container" class="container">
    <%= render :partial=>'/flash_msg', layout: 'shared/flash_messages' %>

    <div class="row">
      <%= yield %>
    </div>
  </div>

  </body>
</html>
