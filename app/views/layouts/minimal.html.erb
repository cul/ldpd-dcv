<!DOCTYPE html>
<html lang="en" class="no-js">
	<head>
		<%= render partial: 'shared/head_includes' %>
		<%- if ['gallery', 'portrait', 'signature'].include?(subsite_layout) -%>
			<%= stylesheet_link_tag *Array(subsite_styles) %>
		<%- else -%>
			<%= stylesheet_link_tag *["gallery-#{Dcv::Sites::Constants.default_palette}", subsite_layout].except('dcv') %>
		<%- end -%>
		<meta name="robots" content="noindex" />
	</head>
	<body class="empty <%= render_subsite_body_classes %> <%= params[:action] %>">
		<%= render :partial => 'shared/topnavbar' %>
		<div id="outer-wrapper" class="container">
			<div class="row">
				<div id="main-container" class="col-md-12 px-0 py-0 mw-100">
					<%= yield %>
				</div>
			</div><!--/.row-->
		</div><!--/#outer-wrapper-->
		<%= render Dcv::FooterComponent.new(subsite: @subsite, repository_id: params[:repository_id]) %>
	</body>
</html>
