<% # container for all documents in index view -%>
<div id="search-results">
  <div class="row">
    <% @items_per_row_counter = 1 %>
    <%= render :partial=>'document',:collection=> documents, :as=> :document, :locals=> {row_offset: 0, show_counter: true} %>
  </div>
</div>
